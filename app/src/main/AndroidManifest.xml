<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
            android:name=".creator.DarkThemeApp"
            android:allowBackup="true"
            android:dataExtractionRules="@xml/data_extraction_rules"
            android:fullBackupContent="@xml/backup_rules"
            android:label="@string/app_name"
            android:supportsRtl="true"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar"
            tools:targetApi="31">
        <activity
                android:name=".player.ui.view.AudioplayerActivity"
                android:exported="false"/>
        <activity
                android:name=".library.ui.view.LibraryActivity"
                android:exported="false"/>
        <activity
                android:name=".search.ui.view.SearchActivity"
                android:exported="false"/>
        <activity
                android:name=".settings.ui.view.SettingsActivity"
                android:exported="false"/>
        <activity
                android:name=".main.ui.view.MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>